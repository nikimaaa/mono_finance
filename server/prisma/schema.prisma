generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Reserve {
  id    Int     @id @default(autoincrement())
  name  String
  price Int?
  link  String?
}